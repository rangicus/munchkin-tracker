# Munchkin Strength Tracker
A web app used to track numbers in the card game: Munchkin.

## Fiddling
The command `npm start` will start a local development server.

## Building
The command `npm run build` wil build a release of the app.